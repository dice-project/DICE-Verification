E<>({% for stage_id,label in finalstages.iteritems() -%} active_stage{{ label }} > {{ indexes[label][stage_id] }} {% if (0 < loop.index) and (loop.index < finalstages|length) %} &&  {% endif -%} {% endfor %} && time<{{ deadline }})